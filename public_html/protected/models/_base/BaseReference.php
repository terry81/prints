<?php

/**
 * This is the model base class for the table "reference".
 * DO NOT MODIFY THIS FILE! It is automatically generated by giix.
 * If any changes are necessary, you must set or override the required
 * property or method in class "Reference".
 *
 * Columns in table "reference" available as properties of the model,
 * followed by relations of table "reference" available as properties of the model.
 *
 * @property integer $fingerprint_id
 * @property string $author
 * @property string $title
 * @property string $journal
 * @property integer $literature_id
 * @property integer $year
 *
 * @property Fingerprint $fingerprint
 */
abstract class BaseReference extends GxActiveRecord {

	public static function model($className=__CLASS__) {
		return parent::model($className);
	}

	public function tableName() {
		return 'reference';
	}

	public static function label($n = 1) {
		return Yii::t('app', 'Reference|References', $n);
	}

	public static function representingColumn() {
		return 'author';
	}

	public function rules() {
		return array(
			array('author', 'required'),
			array('fingerprint_id, year', 'numerical', 'integerOnly'=>true),
			array('author, title', 'length', 'max'=>1500),
			array('journal', 'length', 'max'=>100),
			array('fingerprint_id, title, journal, year', 'default', 'setOnEmpty' => true, 'value' => null),
			array('fingerprint_id, author, title, journal, literature_id, year', 'safe', 'on'=>'search'),
		);
	}

	public function relations() {
		return array(
			'fingerprint' => array(self::BELONGS_TO, 'Fingerprint', 'fingerprint_id'),
		);
	}

	public function pivotModels() {
		return array(
		);
	}

	public function attributeLabels() {
		return array(
			'fingerprint_id' => null,
			'author' => Yii::t('app', 'Author'),
			'title' => Yii::t('app', 'Title'),
			'journal' => Yii::t('app', 'Journal'),
			'literature_id' => Yii::t('app', 'Literature'),
			'year' => Yii::t('app', 'Year'),
			'fingerprint' => null,
		);
	}

	public function search() {
		$criteria = new CDbCriteria;

		$criteria->compare('fingerprint_id', $this->fingerprint_id);
		$criteria->compare('author', $this->author, true);
		$criteria->compare('title', $this->title, true);
		$criteria->compare('journal', $this->journal, true);
		$criteria->compare('literature_id', $this->literature_id);
		$criteria->compare('year', $this->year);

		return new CActiveDataProvider($this, array(
			'criteria' => $criteria,
		));
	}
}
